<div class="container">
  <div class="row centered-form">
    <div class="col-xs-12 col-sm-8 col-md-4 col-sm-offset-2 col-md-offset-4">
      <div class="panel panel-default">

        <div class="panel-heading">
          <h3 class="panel-title">Inicio de Sesión <small>SGMUTP</small></h3>
        </div>

        <div class="panel-body">
          
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <div class= "row">
              <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                  <%= f.label :email, "Correo Electrónico" %><br />
                  <%= f.email_field :email, autofocus: true, class: "form-control input-sm", :required => true %>
                </div>
              </div>
            </div>

            <div class= "row">
              <div class="col-xs-12 col-sm-12 col-md-12">
                <div class="form-group">
                  <%= f.label :password, "Contraseña" %><br />
                  <%= f.password_field :password, autocomplete: "off", class: "form-control input-sm", :required => true %>                  
                </div>
              </div>
            </div>

            <div class= "row">
              <div class="col-xs-12 col-sm-12 col-md-12">
                <% if devise_mapping.rememberable? -%>
                  <div class="form-group">
                    <%= f.check_box :remember_me %>
                    <%= f.label :remember_me, "Recordarme" %>
                  </div>
                <% end -%>
              </div>
            </div>

              <div class="actions">
                <%= f.submit "Iniciar Sesión", class: "btn btn-info btn-block" %>
              </div>
            <% end %>
          
        </div>

      </div>
    </div>
  </div>
</div>

